---
name: Bug report
about: Something doesn't work the way the README says it should
labels: bug
---

## What I expected

<!-- One-line description of the behavior you were expecting. -->

## What happened

<!-- What actually happened. Include error messages verbatim. -->

## Reproduction

```bash
# The exact commands that trigger the bug. If it involves a specific
# source file / RSS feed / URL, paste its contents or a link to it.
```

## Environment

- `node --version` output:
- `contextix --version` output:
- OS + version:
- Extractor mode (regex / agentic / auto):
- Have you set `ANTHROPIC_API_KEY`? (yes / no)

## Graph state (if relevant)

<!-- Contents of ~/.contextix/graph.json, or `contextix signals --json`
output, or a minimal graph that reproduces the issue. -->
